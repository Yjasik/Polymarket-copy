{
  "name": "polymarket-contracts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "node": "hardhat node",
    "clean": "hardhat clean",
    "clear": "rm -rf node_modules package-lock.json test cache artifacts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "5.0.0",
    "hardhat": "2.28.0",
    "dotenv": "16.4.5"
  },
  "dependencies": {
    "@openzeppelin/contracts": "5.0.2"
  }
}
